const express=require('express');const mysql=require('mysql2');const cors=require('cors');const app=express();app.use(cors());const db=mysql.createConnection({host:'mysql',user:'admin',password:'admin123',database:'demo_db'});app.get('/users',(req,res)=>db.query('select * from users',(e,r)=>res.json(r)));app.listen(5000);